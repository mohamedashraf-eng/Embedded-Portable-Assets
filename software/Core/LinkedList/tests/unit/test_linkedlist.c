#include "Wx_Static_LinkedList.h"
#include "unity.h"
#include "unity_fixture.h"

/* Define the test group name for this swc */
TEST_GROUP(LinkedList);

/* Setup for all tests (Pre conditions) */
TEST_SETUP(LinkedList)
{
    ;
}

/* Setup for all tests (Post conditions) */
TEST_TEAR_DOWN(LinkedList)
{
    ;
}

/* Example test case */
TEST(LinkedList, test_linkedlist_XYZ)
{
    TEST_ASSERT_EQUAL(0, 0);
}